interface:
  display_name: "Morphic"
  short_description: "Repo-specific patterns and workflows for morphic"
  default_prompt: "Use the morphic repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true